kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
    extraPortMappings:
      # Authentication API — nodePort 30801 → localhost:8081
      - containerPort: 30801
        hostPort: 8081
        protocol: TCP
