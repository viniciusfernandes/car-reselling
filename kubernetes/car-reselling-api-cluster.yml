
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
    extraPortMappings:
      # Car Reselling API — nodePort 30808 → localhost:8080
      - containerPort: 30808
        hostPort: 8080
        protocol: TCP
