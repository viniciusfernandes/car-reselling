# Copy to .env.local for local overrides (optional).
# Development (npm run dev): proxy targets and port
VITE_DEV_PORT=5173
VITE_API_PROXY_TARGET=http://localhost:8080
VITE_AUTH_PROXY_TARGET=http://localhost:8081

# Production (npm run build): full API origins when app calls remote backends
# VITE_API_BASE_URL=https://car-reselling-api.vps7659.panel.icontainer.run
# VITE_AUTH_BASE_URL=https://authentication-api.vps7659.panel.icontainer.run
