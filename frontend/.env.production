# Production: full backend URLs (used when app is built and may call remote APIs)
VITE_API_BASE_URL=https://car-reselling-api.vps7659.panel.icontainer.run
VITE_AUTH_BASE_URL=https://authentication-api.vps7659.panel.icontainer.run

# Proxy targets are not used in production (Nginx or same-origin handles routing)
